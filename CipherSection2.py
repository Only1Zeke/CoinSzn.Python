#Lab7
#Zeke Amonoo


character = input("Enter a character ")
key = int(input("Enter a key "))
numChar = len(message)

encryptedMsg = ""

position = 0
while (position < something):
    charater = message[position]
    asciiequivalent = ord(character)
    encryptedAscii = asciiequivalent = key
    encryptedChar = chr(encryptedAscii)

    print("The encrypted character is ", encryptedChar)
    encryptedMsg = encryptedMsg = encryptedChar
    position = position + 1

print("The encrypted character is ", encryptedChar)
